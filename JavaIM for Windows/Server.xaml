<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JavaIM_for_Windows.Server"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JavaIM_for_Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <AutoSuggestBox x:Name="ASB" PlaceholderText="搜索服务器" QueryIcon="Find" Width="500" Height="35" VerticalAlignment="Top" Margin="0,10,0,0" />
        <ListView ItemsSource="{x:Bind Servers}" SelectionMode="None" Margin="180,60,150,80">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ServerInfo">
                    <Border BorderBrush="#DDD" CornerRadius="8" BorderThickness="1.5" Margin="0,0,0,20" HorizontalAlignment="Center">
                        <StackPanel>
                            <Border BorderBrush="Aqua" CornerRadius="8" BorderThickness="1.5" Margin="10,10,10,10">
                                <StackPanel Padding="10">
                                    <TextBlock Text="服务器名：" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind ServerName}" TextWrapping="Wrap" Margin="0,0,0,2" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <Border BorderBrush="Aqua" CornerRadius="8" BorderThickness="1.5" Margin="10,10,10,10">
                                <StackPanel Padding="10">
                                    <TextBlock Text="IP：" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind ServerIP}" TextWrapping="Wrap" Margin="0,0,0,2" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <Border BorderBrush="Aqua" CornerRadius="8" BorderThickness="1.5" Margin="10,10,10,10">
                                <StackPanel Padding="10">
                                    <TextBlock Text="端口：" FontSize="14" HorizontalAlignment="Center" />
                                    <TextBlock Text="{x:Bind ServerPort}"  Margin="0,0,0,2" HorizontalAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <Button Style="{StaticResource AccentButtonStyle}" Content="设置为使用的服务器" Margin="5,0,5,5" HorizontalAlignment="Center" Click="SelectServerButton_Click"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Style="{StaticResource AccentButtonStyle}" Content="添加新的服务器" HorizontalAlignment ="Right" VerticalAlignment="Bottom" Margin="0,0,30,30" Click="AddServerButton_Click"/>
        <ContentDialog x:Name="AddServerContentDialog"
           PrimaryButtonText="保存服务器" IsPrimaryButtonEnabled="False" 
           CloseButtonText="取消">
            <ContentDialog.TitleTemplate>
                <DataTemplate>
                    <TextBlock Text="添加新的服务器" HorizontalAlignment="Center"/>
                </DataTemplate>
            </ContentDialog.TitleTemplate>
            <StackPanel>
                <TextBox x:Name="Name" Header="服务器名:" PlaceholderText="JavaIM 服务器名"  TextChanged="Name_TextChanged"/>
                <TextBox x:Name="IP" Header="服务器IP地址:" PlaceholderText="IP地址或域名"  TextChanged="Name_TextChanged"/>
                <NumberBox x:Name="Port"
                           Header="端口:"
                           PlaceholderText="1~65535"
                           SpinButtonPlacementMode="Compact"
                           SmallChange="1"
                           Minimum="1"
                           Maximum="65535"
                           ValueChanged="Port_ValueChanged"
                           />
                <TextBlock Text="" />
                <Button x:Name="ChooseCACert" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center" VerticalContentAlignment="Center" Content="选择CA证书" Click="ChooseCACert_Click"/>
                <TextBlock x:Name="FileNameDisplay" VerticalAlignment="Center" />
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
